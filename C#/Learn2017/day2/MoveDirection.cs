using System;
namespace Move
{
    /// <summary>
    /// 定义枚举类型：移动方向
    /// </summary>
    enum MoveDirection
    {
        // 创建语法：enum 枚举名 {}
        // 以下对创建的数据类型定义值
        Up = 0,
        Down = 1,
        Left = 2,
        Right = 3
    }
}
