// 没提及的运算符基本和其他常用语言相似


// 比较运算符
/* 
    大于等于: >=
    小于等于: <=
    等于: ===
    不等于: !==
*/

// 逻辑运算符
/* 
    与 ( && ): 两个需要都为 true 才能返回 true
    或 ( || ): 两个只要一个为 true 便返回 true, 否则为 false
    非 ( ! ): 取反 -- 把 true 转为 false, 把 false 转为 true
*/

// 三元运算符
/* 
    结果 = 判断条件 ? 值1 : 值2
    判断结果为 真：结果为值1
    判断条件为 假：结果为值2
*/
    let strt0: string = 5<4 ? "yes":"no";
    console.log(strt0)